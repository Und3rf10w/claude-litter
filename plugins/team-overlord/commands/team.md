---
description: List and manage teams
argument-hint: [team-name]
---

Use the team-overlord MCP tools. If $ARGUMENTS is empty, call `list_teams` and display a summary of all teams. If a team name is given, call `get_team` with that name and show full details including members and their configuration.
