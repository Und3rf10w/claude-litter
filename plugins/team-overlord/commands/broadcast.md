---
description: Broadcast message to all agents in a team
argument-hint: <team> <message>
---

Use the `broadcast_message` tool from team-overlord. Parse $ARGUMENTS: first word = team name, remaining text = message content. Both are required — if either is missing, ask the user to provide them.
